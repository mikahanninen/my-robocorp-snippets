tasks:
  Run all tasks:
    shell: python -m robot -L TRACE --report NONE --outputdir output --logtitle "Task log" example.robot

condaConfigFile: ../conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
  - ../shared_resources
ignoreFiles:
  - .gitignore
